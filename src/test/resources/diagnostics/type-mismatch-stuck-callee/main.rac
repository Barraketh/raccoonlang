def twice (b: Bool): Nat := {
  match b returning Nat with
  | Bool.true => Nat.zero
  | Bool.false => Nat.succ(Nat.zero)
}

def bad (b: Bool): Eq(Nat, twice(b), Nat.zero) := Eq.refl(Nat.zero)

{ Nat.zero }
